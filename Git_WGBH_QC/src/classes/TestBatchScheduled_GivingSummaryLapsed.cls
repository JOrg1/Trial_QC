@isTest
Private class TestBatchScheduled_GivingSummaryLapsed
{
    static testMethod void TestBatchScheduled_GivingSummaryLapsed()
    {
        BatchScheduled_GivingSummaryLapsed b= new BatchScheduled_GivingSummaryLapsed();
        b.execute(null);
    }
}